apply plugin: 'com.android.application'

android {
    compileSdkVersion 8
    buildToolsVersion "33.0.0"

    defaultConfig {
        applicationId "com.SAURABH.pizza"
        minSdkVersion 8
        targetSdkVersion 8
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}
